package net.raezorx;

/**
 * User: Raezor
 * Date: 4/7/2014
 * Time: 10:21 PM
 */
public class Reactor {
    /*
    static boolean running = true;
    static boolean shuttingDown = false;

    static final List<Bot> bots = new LinkedList<Bot>();

    public static Bot createBot(String server, int port)
    {
        if (shuttingDown)
            throw IllegalStateException();

        final Bot b = new Bot(...);

        synchronized(bots)
        {
            bots.put(b);
        }

        return b;
    }

    public static void run()
    {
        if (running)
            throw IllegalStateException();

        running = true;
        long now = System.currentTimeMillis();

        while(running)
        {
            if (System.currentTimeMillis() - now > 1000)
                executeTimers();

            if (hasRead())
                processReads()

            if (hasWrites())
                processWrites();

            cleanup();
        }
    }

    public static void shutdown()
    {
        shuttingDown = true;
    }   */

}